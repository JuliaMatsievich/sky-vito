import { centralContainer, title } from '../../styles/commonStyles.styles';
import styled from 'styled-components';

export const MainTitle = styled.h1`
  ${title};
  padding-bottom: 10px;
`;

export const MainContainer = styled.div`
  ${centralContainer}
`;
